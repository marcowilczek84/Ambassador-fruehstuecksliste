s"><button className="modal-action" onClick={() => { setSelected(null); if (guestInfoEditOnly) { setGuestInfoEditOnly(false); setEditMode(false); } }}>{c.cancel}</button><button className="modal-action primary" onClick={() => saveRoomDetails(selected)}>{c.changesSave}</button></div>
          </div> : selected.departed ? <div className="modal-body completed-room-body"><div className="completed-room-message"><CheckCircle2 /><div><strong>{tr("Frühstück beendet", "Breakfast finished", "Đã kết thúc bữa sáng")}</strong><span>{tr("Dieses Zimmer ist für heute abgeschlossen und zählt nicht mehr als offen.", "This room is completed for today and no longer counts as open.", "Phòng này đã hoàn tất hôm nay và không còn được tính là đang chờ.")}</span></div></div><div className="modal-actions"><button className="modal-action primary full" onClick={() => setSelected(null)}>{c.close}</button></div></div> : !selected.present ? <div className="modal-body checkin-choice-body">
            {selected.people > 1 && <div className="checkin-count-block"><span className="choice-label">{tr("Wie viele Personen kommen jetzt zum Frühstück?", "How many people are arriving for breakfast now?", "Có bao nhiêu người đến ăn sáng bây giờ?")}</span><div className="checkin-quantity-grid">{Array.from({ length: selectedRemaining || selected.people }, (_, index) => { const value = index + 1; return <button key={value} type="button" className={checkInCount === value ? "active" : ""} onClick={() => setCheckInCount(value)}>{value} {tr(value === 1 ? "Person" : "Personen", value === 1 ? "person" : "people", "người")}</button>; })}</div><p className="helper-copy">{tr("Weitere Personen kannst du später einchecken.", "You can check in the remaining guests later.", "Bạn có thể check-in những khách còn lại sau.")}</p></div>}
            <button className={`room-service-option ${selected.table === "Roomservice" ? "selected" : ""}`} onClick={() => setSelected({ ...selected, table: "Roomservice" })}><BedDouble /><span><strong>Roomservice</strong><small>{tr("Frühstück wird auf das Zimmer gebracht", "Breakfast is delivered to the room", "Bữa sáng được mang lên phòng")}</small></span>{selected.table === "Roomservice" && <Check />}</button>
            <div className="choice-separator"><span>{tr("oder Tisch wählen", "or choose a table", "hoặc chọn bàn")}</span></div>
            <div className="table-picker">{Array.from({ length: 50 }, (_, index) => <button type="button" className={selected.table === `Tisch ${index + 1}` ? "selected" : ""} key={index} onClick={() => setSelected({ ...selected, table: `Tisch ${index + 1}` })}>{index + 1}</button>)}</div>
            <div className="group-link group-link-button">
              <button type="button" className="group-link-trigger" onClick={() => setGroupRoomPickerOpen(true)}>
                <Plus />
                <span>
                  <strong>{groupRoom ? tr("Weiteres Zimmer ändern", "Change additional room", "Đổi phòng thêm") : tr("Weiteres Zimmer hinzufügen", "Add another room", "Thêm phòng khác")}</strong>
                  <small>{groupRoom ? tr(`Zimmer ${groupRoom.room} · ${groupRoom.guests[0]}`, `Room ${groupRoom.room} · ${groupRoom.guests[0]}`, `Phòng ${groupRoom.room} · ${groupRoom.guests[0]}`) : tr("Falls ein weiteres Zimmer zur Gruppe gehört", "If another room belongs to this group", "Nếu có phòng khác thuộc cùng nhóm")}</small>
                </span>
                {groupRoom && <span className="group-link-selected">✓</span>}
              </button>
              {groupRoom && <div className="group-link-summary">
                <span><strong>{tr("Zimmer", "Room", "Phòng")} {groupRoom.room}</strong><small>{groupRemaining} {tr(groupRemaining === 1 ? "Person" : "Personen", groupRemaining === 1 ? "person" : "people", "người")}</small></span>
                <button type="button" onClick={() => setGroupRoomNumber("")}>{tr("Entfernen", "Remove", "Xóa")}</button>
              </div>}
            </div>

            {groupRoomPickerOpen && <div className="group-room-popup-layer" onClick={() => setGroupRoomPickerOpen(false)}>
              <section className="group-room-popup" onClick={e => e.stopPropagation()}>
                <header className="group-room-popup-head">
                  <div><span>{tr("Gruppe", "Group", "Nhóm")}</span><h3>{tr("Weiteres Zimmer hinzufügen", "Add another room", "Thêm phòng khác")}</h3><p>{tr("Wähle das Zimmer, das gemeinsam platziert werden soll.", "Choose the room that should be seated together.", "Chọn phòng sẽ được xếp cùng nhau.")}</p></div>
                  <button type="button" onClick={() => setGroupRoomPickerOpen(false)} aria-label={tr("Schließen", "Close", "Đóng")}><X /></button>
                </header>
                <div className="group-room-popup-list">
                  {rooms.filter(room => room.room !== selected.room && room.guests.length > 0 && getRemainingCount(room) > 0 && !roomIsDeparted(room)).map(room => {
                    const isChosen = groupRoomNumber === String(room.room);
                    const remaining = getRemainingCount(room);
                    return <button key={room.room} type="button" className={isChosen ? "chosen" : ""} onClick={() => { setGroupRoomNumber(String(room.room)); setGroupRoomPickerOpen(false); }}>
                      <span className="group-popup-number">{room.room}</span>
                      <span className="group-popup-copy"><strong>{room.guests[0]}</strong><span className="group-popup-meta"><small>{remaining} {tr(remaining === 1 ? "Person" : "Personen", remaining === 1 ? "person" : "people", "người")}</small><em className={room.included ? "included" : "not-included"}>{room.included ? tr("Inklusive", "Included", "Bao gồm") : tr("Nicht inklusive", "Not included", "Không bao gồm")}</em></span></span>
                      <span className="group-popup-check">{isChosen ? "✓" : ""}</span>
                    </button>;
                  })}
                </div>
                <button type="button" className="group-room-popup-cancel" onClick={() => setGroupRoomPickerOpen(false)}>{tr("Abbrechen", "Cancel", "Hủy")}</button>
              </section>
            </div>}
            <div className="modal-actions"><button className="modal-action" onClick={() => setSelected(null)}>{c.cancel}</button><button className="modal-action primary" disabled={!selected.table} onClick={() => groupRoom ? checkInGroup(selected, Math.min(checkInCount, selectedRemaining || selected.people), groupRoom) : checkInGuests(selected, Math.min(checkInCount, selectedRemaining || selected.people))}>{groupRoom ? tr(`${Math.min(checkInCount, selectedRemaining || selected.people) + groupRemaining} Personen gemeinsam einchecken`, `Check in ${Math.min(checkInCount, selectedRemaining || selected.people) + groupRemaining} people together`, `Check-in cùng ${Math.min(checkInCount, selectedRemaining || selected.people) + groupRemaining} người`) : tr(`${Math.min(checkInCount, selectedRemaining || selected.people)} ${Math.min(checkInCount, selectedRemaining || selected.people) === 1 ? "Person" : "Personen"} einchecken`, `Check in ${Math.min(checkInCount, selectedRemaining || selected.people)} ${Math.min(checkInCount, selectedRemaining || selected.people) === 1 ? "person" : "people"}`, `Check-in ${Math.min(checkInCount, selectedRemaining || selected.people)} người`)}</button></div>
          </div> : <div className="modal-body">
            <div className="stay-strip"><span>{selected.breakfastDay ? `${selected.breakfastDay}. ${c.breakfast}` : tr("Frühstückstag offen", "Breakfast day not set", "Chưa xác định ngày ăn sáng")}</span><strong>{stayText(selected)}</strong></div>
            <div className="detail-grid"><div className="detail-item"><span>{c.guests}</span><strong>{selected.people || "–"}</strong></div><div className="detail-item"><span>{c.breakfast}</span><strong>{selected.included ? tr("Inklusive", "Included", "Đã bao gồm") : c.notIncluded}</strong></div><div className="detail-item"><span>{tr("Tisch / Service", "Table / service", "Bàn / dịch vụ")}</span><strong>{selected.table || c.notSelected}</strong></div></div>
            {selected.note && <div className="important-note"><strong>{tr("Hinweis", "Note", "Lưu ý")}</strong><p>{selected.note}</p></div>}
            {selectedRemaining > 0 && <div className="pending-checkin-card"><strong>{tr("Noch offen", "Still open", "Còn chờ")}: {selectedRemaining} {tr(selectedRemaining === 1 ? "Person" : "Personen", selectedRemaining === 1 ? "person" : "people", "người")}</strong><p>{tr("Wenn noch jemand später kommt, kannst du ihn hier direkt einchecken.", "If someone arrives later, you can check them in right here.", "Nếu có người đến sau, bạn có thể check-in ngay tại đây.")}</p><div className="checkin-quantity-grid compact">{Array.from({ length: selectedRemaining }, (_, index) => { const value = index + 1; return <button key={value} type="button" className={checkInCount === value ? "active" : ""} onClick={() => setCheckInCount(value)}>{value}</button>; })}</div><button className="modal-action primary full" onClick={() => checkInGuests(selected, Math.min(checkInCount, selectedRemaining))}>{tr(`${Math.min(checkInCount, selectedRemaining)} ${Math.min(checkInCount, selectedRemaining) === 1 ? "Person" : "Personen"} zusätzlich einchecken`, `Check in ${Math.min(checkInCount, selectedRemaining)} more ${Math.min(checkInCount, selectedRemaining) === 1 ? "person" : "people"}`, `Check-in thêm ${Math.min(checkInCount, selectedRemaining)} người`)}</button></div>}
            <div className="modal-actions"><button className="modal-action" onClick={() => setSelected(null)}>{c.close}</button><button className="modal-action danger" onClick={() => togglePresence(selected)}>{c.leave}</button></div>
          </div>}
        </section>
      </div>}

      {checkinInfo && <div className="modal-layer info-layer" onClick={() => setCheckinInfo(null)}><section className="modal checkin-card" onClick={e => e.stopPropagation()}><div className="checkin-mark"><Check /></div><span className="modal-kicker">{tr("Check-in erfolgreich", "Check-in successful", "Check-in thành công")}</span><h2>{checkinInfo.guests[0]}</h2>{checkinInfo.guests.length > 1 && <p className="checkin-companions">{checkinInfo.guests.slice(1).join(" · ")}</p>}<p>{tr("Zimmer", "Room", "Phòng")} {checkinInfo.room}</p><div className="checkin-facts">
        <div className="checkin-fact"><BedDouble /><span><small>{tr("Tisch / Service", "Table / service", "Bàn / dịch vụ")}</small><strong>{checkinInfo.table}</strong></span></div>
        <div className="checkin-fact"><Coffee /><span><small>{tr("Frühstück im Aufenthalt", "Breakfast during this stay", "Bữa sáng trong kỳ lưu trú")}</small><strong>{checkinInfo.breakfastDay || 1}. {tr("Frühstück", "breakfast", "bữa sáng")}{checkinInfo.included ? ` · ${tr("inklusive", "included", "đã bao gồm")}` : ""}</strong></span></div>
        <div className="checkin-fact"><CalendarDays /><span><small>{tr("Aufenthalt", "Stay", "Lưu trú")}</small><strong>{stayText(checkinInfo)}</strong></span></div>
        <div className="checkin-fact"><History /><span><small>{tr("Bisherige Aufenthalte", "Previous stays", "Các lần lưu trú trước")}</small><strong>{checkinInfo.previousStays?.length ? tr(`Zuletzt hier: ${checkinInfo.previousStays.at(-1)}`, `Last stayed: ${checkinInfo.previousStays.at(-1)}`, `Lần gần nhất: ${checkinInfo.previousStays.at(-1)}`) : tr("Erster gespeicherter Aufenthalt", "First recorded stay", "Lần lưu trú đầu tiên được ghi nhận")}</strong></span></div>
      </div>{checkinInfo.note && <div className="important-note info-note"><CircleAlert /><span><strong>{tr("Wichtiger Hinweis", "Important note", "Lưu ý quan trọng")}</strong><p>{checkinInfo.note}</p></span></div>}<button className="modal-action primary full" onClick={() => setCheckinInfo(null)}>{tr("Verstanden", "Got it", "Đã hiểu")}</button></section></div>}

      {panel === "stats-menu" as any && <div className="modal-layer" onClick={() => setPanel(null)}><section className="modal stats-menu-modal" onClick={e => e.stopPropagation()}><header className="modal-head"><div><span className="modal-kicker">{tr("Auswertung", "Insights", "Thống kê")}</span><h2>{tr("Statistik", "Statistics", "Thống kê")}</h2><p>{tr("Welche Auswertung möchtest du öffnen?", "Which view would you like to open?", "Bạn muốn mở thống kê nào?")}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body"><div className="stats-menu-options"><button className="stats-menu-option" onClick={() => setPanel("stats")}><BarChart3 /><span><strong>{c.dayStats}</strong><small>{tr("Heute im Überblick", "Today at a glance", "Tổng quan hôm nay")}</small></span></button><button className="stats-menu-option" onClick={() => setPanel("week")}><CalendarDays /><span><strong>{tr("Wochenstatistik", "Weekly statistics", "Thống kê tuần")}</strong><small>{tr("Letzte sieben Abschlüsse", "Last seven completed days", "Bảy ngày đã hoàn tất gần nhất")}</small></span></button></div></div></section></div>}
      {panel === "stats" && <div className="modal-layer" onClick={() => setPanel(null)}><section className="modal stats-modal" onClick={e => e.stopPropagation()}><header className="modal-head"><div><span className="modal-kicker">{tr("Heute", "Today", "Hôm nay")}</span><h2>{c.dayStats}</h2><p>{c.statsIntro}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body"><div className="stats-hero"><div><strong>{attendanceRate}%</strong><span>{tr("der inklusiven Gäste erschienen", "of included guests arrived", "khách bao gồm đã đến")}</span></div><div><strong>{includedShare}%</strong><span>{tr("der erschienenen Gäste waren inklusive", "of guests who arrived were included", "khách đã đến thuộc diện bao gồm")}</span></div></div><h3 className="stats-subtitle">{tr("Gästeaufkommen", "Guest volume", "Lượng khách")}</h3><div className="stat-list"><p><span>{c.includedCame}</span><strong>{includedArrived} {tr("von", "of", "trên")} {includedGuests}</strong></p><p><span>{c.includedNoShow}</span><strong>{includedNoShow}</strong></p><p><span>{c.extraGuests}</span><strong>{additionalArrived}</strong></p><p><span>{c.breakfastsTotal}</span><strong>{arrivedTotal}</strong></p></div><h3 className="stats-subtitle">{tr("Ankunftsprofil", "Arrival profile", "Khung giờ khách đến")}</h3><div className="timeline-list"><p><Clock3/><span>{c.firstGuest}</span><strong>{timeText(sortedArrivals[0]?.at)}</strong></p><p><Clock3/><span>{tr("Stärkstes 30-Minuten-Fenster", "Busiest 30-minute window", "30 phút đông nhất")}</span><strong>{peak.label} · {peak.people} {c.guests}</strong></p><p><Clock3/><span>{tr("80% aller Gäste kamen zwischen", "80% of guests arrived between", "80% khách đến trong khoảng")}</span><strong>{eightyWindow}</strong></p><p><Clock3/><span>{c.lastGuest}</span><strong>{timeText(sortedArrivals.at(-1)?.at)}</strong></p></div></div></section></div>}
      {panel === "week" && <div className="modal-layer" onClick={() => setPanel(null)}><section className="modal stats-modal" onClick={e => e.stopPropagation()}><header className="modal-head"><div><span className="modal-kicker">{tr("Letzte 7 Tage", "Last 7 days", "7 ngày gần nhất")}</span><h2>{tr("Wochenstatistik", "Weekly statistics", "Thống kê tuần")}</h2><p>{tr("Die vergangenen sieben Abschlüsse im Vergleich", "Comparison of the last seven completed days", "So sánh bảy ngày đã hoàn tất")}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body">{history.length ? <><div className="week-summary"><div><strong>{Math.round(history.reduce((sum,d)=>sum+(d.includedExpected ? d.includedArrived/d.includedExpected*100 : 0),0)/history.length)}%</strong><span>{tr("Ø Anwesenheitsquote", "Average attendance", "Tỷ lệ đến trung bình")}</span></div><div><strong>{Math.round(history.reduce((sum,d)=>sum+d.includedArrived+d.additional,0)/history.length)}</strong><span>{tr("Ø Frühstücke pro Tag", "Average breakfasts per day", "Trung bình bữa sáng mỗi ngày")}</span></div><div><strong>{history.length}</strong><span>{tr("Abschlüsse", "Completed days", "Ngày đã hoàn tất")}</span></div></div><div className="week-list">{history.map(day => <div key={day.date}><time>{new Intl.DateTimeFormat(locale,{weekday:"short",day:"2-digit",month:"2-digit"}).format(new Date(`${day.date}T12:00:00`))}</time><span><strong>{day.includedArrived+day.additional}</strong> {c.guests}</span><span>{day.includedExpected ? Math.round(day.includedArrived/day.includedExpected*100) : 0}% {tr("inklusive erschienen", "included arrived", "khách bao gồm đã đến")}</span><small>{tr("Peak", "Peak", "Cao điểm")} {day.peak} · 80% {day.eighty}</small></div>)}</div></> : <div className="empty-stats"><CalendarDays/><h3>{tr("Noch keine abgeschlossenen Tage", "No completed days yet", "Chưa có ngày nào hoàn tất")}</h3><p>{tr("Nach dem ersten Tagesabschluss beginnt hier der Wochenvergleich.", "The weekly comparison starts after the first completed day.", "So sánh tuần sẽ bắt đầu sau khi hoàn tất ngày đầu tiên.")}</p></div>}</div></section></div>}
      {panel === "import" && <div className="modal-layer"><section className="modal import-modal"><header className="modal-head"><div><h2>{c.reviewMews}</h2><p>{tr("Kontrolliere die erkannten Daten vor der Übernahme.", "Check the detected data before accepting the list.", "Kiểm tra dữ liệu đã nhận diện trước khi sử dụng.")}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body"><div className="import-summary"><span><Coffee /><strong>{importPreview.filter(room => room.included).reduce((sum, room) => sum + room.people, 0)}</strong> {tr("inklusive", "included", "bao gồm")}</span><span><Building2 /><strong>{importPreview.filter(room => room.guests.length).length}</strong> {c.roomsOccupied}</span><span><Users /><strong>{importPreview.reduce((sum, room) => sum + room.people, 0)}</strong> {tr("Gäste gesamt", "guests total", "tổng số khách")}</span></div><div className="import-list">{importPreview.filter(room => room.guests.length).map(room => <div key={room.room} className={room.included ? "included" : ""}><strong>{room.room}</strong><span>{room.guests.join(" · ")}</span><small>{room.people} {room.people === 1 ? tr("Gast", "guest", "khách") : c.guests}{room.included ? ` · ${tr("inklusive", "included", "bao gồm")}` : ""}</small></div>)}</div><div className="modal-actions"><button className="modal-action" onClick={() => setPanel(null)}>{c.cancel}</button><button className="modal-action primary" onClick={confirmImport}>{c.acceptList}</button></div></div></section></div>}
      {panel === "add" && <div className="modal-layer" onClick={() => setPanel(null)}><section className="modal" onClick={e => e.stopPropagation()}><header className="modal-head"><div><h2>{c.addRoom}</h2><p>{c.addGuests}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body"><div className="form-grid"><label><span>{tr("Zimmer", "Room", "Phòng")}</span><select value={newRoom.room} onChange={e => setNewRoom(value => ({ ...value, room: e.target.value }))}><option value="">{c.chooseRoom}</option>{rooms.filter(room => !room.guests.length).map(room => <option key={room.room} value={room.room}>{room.room}</option>)}</select></label><label><span>{c.people}</span><input type="number" min="1" max="8" value={newRoom.people} onChange={e => setNewRoom(value => ({ ...value, people: e.target.value }))} /></label><label className="wide"><span>{c.guestNames}</span><textarea rows={3} value={newRoom.guests} onChange={e => setNewRoom(value => ({ ...value, guests: e.target.value }))} placeholder={c.oneName} /></label><label className="check-field wide"><input type="checkbox" checked={newRoom.included} onChange={e => setNewRoom(value => ({ ...value, included: e.target.checked }))} /><span>{c.breakfastIncluded}</span></label></div><button className="modal-action primary full" disabled={!newRoom.room || !newRoom.guests.trim()} onClick={() => void addRoom()}>{c.createRoom}</button></div></section></div>}
      {panel === "delete" && <div className="modal-layer" onClick={() => setPanel(null)}><section className="modal" onClick={e => e.stopPropagation()}><header className="modal-head"><div><h2>{c.deleteSure}</h2><p>{c.deleteHint}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body"><div className="modal-actions"><button className="modal-action" onClick={() => setPanel(null)}>{c.cancel}</button><button className="modal-action danger" onClick={() => void clearList()}>{c.delete}</button></div></div></section></div>}
      {panel === "finish" && <div className="modal-layer" onClick={() => setPanel(null)}><section className="modal compact-summary" onClick={e => e.stopPropagation()}><header className="modal-head"><div><span className="modal-kicker">{c.dayClose}</span><h2>{c.endTitle}</h2><p>{tr("Kurzer Überblick vor dem Speichern.", "A short summary before saving.", "Tóm tắt ngắn trước khi lưu.")}</p></div><button className="close-button" onClick={() => setPanel(null)} aria-label={c.close}><X /></button></header><div className="modal-body"><div className="summary-lead"><strong>{arrivedTotal}</strong><span>{tr("Gäste erschienen", "guests arrived", "khách đã đến")}</span></div><div className="summary-lines"><p><span>{c.includedCame}</span><strong>{includedArrived} / {includedGuests} · {attendanceRate}%</strong></p><p><span>{c.includedNoShow}</span><strong>{includedNoShow}</strong></p><p><span>{c.extraGuests}</span><strong>{additionalArrived}</strong></p><p><span>{c.peak}</span><strong>{peak.label}</strong></p><p><span>{tr("80% der Gäste", "80% of guests", "80% khách")}</span><strong>{eightyWindow}</strong></p></div><div className="modal-actions"><button className="modal-action" onClick={() => setPanel(null)}>{c.keepWorking}</button><button className="modal-action primary" onClick={finishDay}>{tr("Tagesabschluss speichern", "Save daily summary", "Lưu tổng kết ngày")}</button></div></div></section></div>}
    </main>
  );
}
