# Recovery status

Do not connect or deploy this repository yet.

The production app at ambassador-fruehstuecksliste.vercel.app was deployed through the Vercel CLI with uncommitted local changes (gitDirty=1). The GitHub commit recorded by Vercel does not build by itself and is not identical to the live application.

The recoverable source files from the recorded commit have been copied here for reconstruction. The existing production deployment must remain untouched until the live-only changes have been reconstructed and a clean build has passed.
